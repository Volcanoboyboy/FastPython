# 赋值运算符 += -= /= //= %= *= **=
num = 2
num += 2 # num = num + 2
num *= 2 # num = num * 2 其他的类似

# 比较运算符 == != > >= < <=

# 逻辑运算符
# and 与， or 或， not 非
print(f"{100 and '100'} | {'100' or False} | {not False}")
